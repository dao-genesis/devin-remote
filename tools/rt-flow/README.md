# rt-flow v3.16.0 · 冷启动辅助插件

> 道法自然 · 账号池流转。冷启动模块的核心依赖，用于多 Devin 账号统一管理与自动注入。

## 功能

- 多 Devin 账号池管理与流转
- 自动注入 `devin-session-token$` 等运行时凭证
- 配合 `RUNBOOK_coldstart.md` 实现 Devin Desktop 一键冷启动

## 安装

```powershell
code --install-extension rt-flow-3.16.0.vsix --force
```
